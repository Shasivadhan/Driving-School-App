spring:
  datasource:
    url: jdbc:mysql://localhost:3306/driving_school?useSSL=false&serverTimezone=Asia/Kolkata
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: false

  thymeleaf:
    cache: false

server:
  port: 8080

logging:
  level:
    root: INFO
    org.springframework.security: INFO

aadhar:
  salt: "change-this-to-a-strong-random-secret"
